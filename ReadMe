# Welcome to Spis UI API

Why you should use this Library
a new drawString() that provides access to using
custom ttf fonts, animated strings, alignment settings, bounding box/auto wrapping text

Aswell as new components to allow for colorfully animated text
